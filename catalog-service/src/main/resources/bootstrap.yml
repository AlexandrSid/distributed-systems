spring:
  application:
    name: catalog-service
  cloud:
    config:
      uri: http://localhost:8888
  config:
    import: configserver
#
#server:
#  port: 8081


#management.endpoints.web.exposure.include: "*"